from app.tools.calculator import calculator
from app.tools.search import search

TOOLS = {
    "calculator": calculator,
    "search": search,
}

# this is a phone book for all the tools that the ai agent uses
